@model ViewStoreOrdersViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <form asp-action="ViewStoreOrders">
        <div class="form-group">
            <label>
                Store
            </label>
            <select asp-for="StoreId" asp-items="@ViewBag.Stores">
            </select>
        </div>
        <div class="form-group">
            <input type="submit" value="Submit" class="btn btn-primary" />
        </div>
    </form>
    @if (ViewBag.StoreOrders != null)
    {
        <table style="width:100%">
            <tr>
                <th>Customer Name</th>
                <th>Order Date</th>
                <th>Price</th>
            </tr>

            @foreach (var order in (List<PizzaBox.Domain.Entities.Order>)ViewBag.StoreOrders)
            { //html table att to display orders w3schools

                <tr>
                    <td>@order.Customer.CustomerName</td>
                    <td>@order.OrderDate.ToString()</td>
                    <td>@order.GetTotal().ToString("c2")</td>
                </tr>
            }
        </table>
    }


</div>
